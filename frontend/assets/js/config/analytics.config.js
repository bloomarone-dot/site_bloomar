/**
 * Bloomarone — Configuration Analytics & Monitoring
 * Modifier uniquement ce fichier pour les identifiants GA4 et Clarity.
 */
window.BloomarAnalyticsConfig = {
    /** Google Analytics 4 — ex. G-XXXXXXXXXX (laisser vide pour désactiver) */
    GA_MEASUREMENT_ID: "",

    /** Microsoft Clarity — ex. abcdefghij (laisser vide pour désactiver) */
    CLARITY_PROJECT_ID: "",

    /** Désactiver tout le module sans retirer le code */
    enabled: true,

    /** Envoyer automatiquement la page vue au chargement */
    autoPageView: true,

    /** anonymiser les IP (GA4) */
    anonymizeIp: true,
};
